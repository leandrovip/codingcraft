@model IEnumerable<CodingCraft.Ex01.Models.Fornecedor>

@{
    ViewBag.Title = "Fornecedor";
}

<h2>Cadastro de Fornecedores</h2>

<br/>

<div class="form-inline">
    @Html.ActionLink("Incluir", "Create", null, new {@class = "btn btn-primary"})

    <div class="form-group">
        @using (Html.BeginForm("Pesquisar", "Fornecedor"))
        {
            <input type="text" name="pesquisa" id="pesquisa" class="form-control" placeholder="Informe o Fornecedor"/>
            <button type="submit" class="btn btn-default">Pesquisar</button>
        }
    </div>
</div>

<br/>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RazaoSocial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataCadastro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioCadastro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataEdicao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioEdicao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RazaoSocial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataCadastro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioCadastro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataEdicao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioEdicao)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.FornecedorId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.FornecedorId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.FornecedorId })
        </td>
    </tr>
}

</table>
